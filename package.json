{
  "name": "cred-mel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:backend": "cd backend && npm ci",
    "build": "cd backend && npm run build && cd backend && npx prisma generate",
    "start": "node backend/dist/index.js",
    "postinstall": "npm run install:backend && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "file-saver": "^2.0.5"
  }
}
